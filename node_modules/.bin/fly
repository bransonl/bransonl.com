#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fly/bin/index" "$@"
  ret=$?
else 
  node  "$basedir/../fly/bin/index" "$@"
  ret=$?
fi
exit $ret
